type: bulk
args:
- type: run_sql
  args:
    sql: |
      create table hge_tests.test_t1(
          c1 int,
          c2 text,
          c3 jsonb
      );
- type: track_table
  args:
    schema: hge_tests
    name: test_t1
- type: run_sql
  args:
    sql: |
      create table hge_tests."testQuoted"(
          "tC1" int,
          "tC2" text
      );
- type: track_table
  args:
    schema: hge_tests
    name: testQuoted
