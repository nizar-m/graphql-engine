type: update_remote_relationship
args:
  name: userBasic
  table: profiles
  hasura_fields:
    - id
  remote_schema: prefixer-proxy
  remote_field:
    prefixer_proxy_users_by_pk:
      arguments:
        id: "$id"
