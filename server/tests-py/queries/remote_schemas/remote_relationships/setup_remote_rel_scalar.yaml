type: create_remote_relationship
args:
  name: userScalar
  table: profiles
  hasura_fields:
    - id
    - alias
  remote_schema: user
  remote_field:
    user:
      arguments:
        id: $id
      field:
        capitalize:
          arguments:
            name: $alias
