description: Remote relationship GraphQL query with errors
url: /v1/graphql
status: 200
response:
  data:
    messages_by_pk:
      id: 1
      message: (from table) You win
      usersNestedArr:
        - provider: provider1
query:
  query: |
    query {
      messages_by_pk(id: 1) {
        id
        message
        usersNestedArr {
          provider
        }
      }
    }
